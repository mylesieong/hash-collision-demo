# Demo a simplest Hash Collision

Hash Collision affects almost every system and field like:

* Password hack

* Fake signature

* Malfunction of Map-liked Data Structure/ Service

In this demo, I create MyHashMap is act like a map with a poor hash function that accept string as input parameter and output hash value (int) with the code point of string's initial charater.
